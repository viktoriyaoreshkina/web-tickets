package com.epam.vo.servlet.reservation;

/**
 * проверка, что в поле "количество бронируемых билетов" стоит не ноль и не пусто
 */
public class TestCountResrvation {
   /* public static boolean addUser(User user){
        boolean result = false;
        if ((!users.containsKey(user.getUser()))&&(user.getPassword()!=null)&&(!"".equals(user.getPassword()))){
            users.put(user.getUser(),user);
            result = true;
        }
        return result;
    }*/
}
